{
  "name": "codex-for-learning-math",
  "version": "1.0.0",
  "private": false,
  "description": "考研数学一交互公式手册 / Interactive Math I formula handbook",
  "license": "MIT",
  "scripts": {
    "check:syntax": "node --check handbook/app.js && node --check handbook/formula-data.js && node --check handbook/study-layer.js && node --check handbook/validate-data.js && node --check handbook/generate-docs.js && node --check handbook/smoke-test.js && node --check handbook/quality-check.js",
    "validate": "node handbook/validate-data.js",
    "docs": "node handbook/generate-docs.js",
    "smoke": "node handbook/smoke-test.js",
    "quality": "node handbook/quality-check.js",
    "verify": "npm run check:syntax && npm run validate && npm run docs && npm run smoke && npm run quality"
  },
  "keywords": [
    "math",
    "kaoyan",
    "formula-handbook",
    "mathjax",
    "github-pages",
    "education"
  ]
}
